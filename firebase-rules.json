{
  "rules": {

    "agents": {
      ".read": "auth != null",
      "$agentId": {
        ".write": true
      }
    },

    "societes": {
      "$societeId": {
        ".read": "auth != null && auth.uid == $societeId",
        ".write": "auth != null && auth.uid == $societeId",
        "agents": {
          "$agentId": {
            ".read": "auth != null && auth.uid == $societeId",
            ".write": true
          }
        }
      }
    },

    "pending": {
      "$deviceId": {
        ".read": true,
        ".write": true,
        "status": {
          ".write": "auth != null || data.val() == 'pending'"
        }
      }
    },

    "companies": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },

    "licences_gps": {
      ".read": false,
      ".write": false
    },

    "uninstall_alerts": {
      "$agentId": {
        ".read": "auth != null",
        ".write": true
      }
    },

    "destruction_commands": {
      "$agentId": {
        ".read": true,
        ".write": "auth != null"
      }
    }
  }
}
