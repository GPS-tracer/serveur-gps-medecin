services:
  - type: web
    name: serveur-gps-medecin
    env: node
    buildCommand: npm install && npm run build:css
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
